client:UI
toc:Controller
db:Server[p]
file:Server[p]
mail:Server[p]
web:Server[p]

client:toc.NewBackup()
toc:db.NewBackup()
toc:file.NewBackup()
toc:mail.NewBackup()
toc:web.NewBackup()

db:toc.UpdateBackup(backup)
file:toc.UpdateBackup(backup)
mail:toc.UpdateBackup(backup)
web:toc.UpdateBackup(backup)

client:toc.GetBackups()

client:toc.GetBackupByID(backup.id)


